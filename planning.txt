Class GPT()
	def __init__(): # conversation
	def run(self, input: str, gpt-model: str, checkbox): # will run in "stream" mode
		pass
	
	@staticmethod
	def zero_shot(input: str, system_message = "you're a helpful assistant", gpt_model = "gpt-3.5-turbo") #no context run
		return answer

	@staticmethod
	def __evaluate():
	
	@staticmethod
	def __conversation():

	# generate web results
	@staticmethod
	def __web_search():

	# generate an output that could be used as output for text
	@staticmethod
	def __to_others(length_wanted):

GPT().zero_shot()